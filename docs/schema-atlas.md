# Schema Atlas

`/schema` renders `GET /api/meta/schema-graph`, generated from SQLAlchemy metadata. It shows table nodes, foreign-key edges, domain filters, and table columns.

The schema currently includes maritime read models, evidence observations, ingestion job/log tables, risk and sanctions tables, news tables, reference data, and PostGIS-backed geo layer metadata.
