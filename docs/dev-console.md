# Dev Console

`/dev` is the operational control surface for V1. Write actions remain under `/api/dev/*`.

It exposes source health, live ingestion controls, table counts, recent observations, jobs, and logs.
